import Directory from "../../compoments/directory/directory.component";

const Home = () => {
  return (
    <div>
      {/* <Directory categories={categories} />; */}
      <Directory />;
    </div>
  );
};

export default Home;
